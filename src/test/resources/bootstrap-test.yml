# Disable some cloud stuff when running tests
micronaut:
  metrics:
    enabled: false
    export:
      logging:
        enabled: true
      cloudwatch:
        enabled: false

  config-client:
    enabled: false